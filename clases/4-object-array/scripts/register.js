// create project with title, description, rate (1-5)
function createProject(title = "Untitled") {
  // todo:
}

// show  title, description and rate (1-5) for project
function showProject(project) {
  // todo:
}

let project = createProject("Mi proyecto");

showProject(project);
